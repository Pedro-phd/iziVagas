import { Story, Meta } from '@storybook/react'
import ToggleLanguage from '.'

export default {
  title: 'ToggleLanguage',
  component: ToggleLanguage
} as Meta

export const Default: Story = () => <ToggleLanguage />
